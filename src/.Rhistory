pairs <- read.table('../../scratch/34-mammals/all.pairs',sep='|')
pairs
plot(hclust(pairs))
dim(pairs)
pairs[,1]
pairs[,2]
pairs[1,]
help(hclust)
library(tidyr)
install.packages('tidyr')
dim(pairs)
spread(pairs, y, z)
library(tidyr)
spread(pairs, y, z)
tmp <- data.frame(x=pairs[,1],y=pairs[,2],z=pairs[,3])
spread(tmp, y, z)
hclust(spread(tmp, y, z))
hclust(spread(tmp, y, z),na.rm=True)
hclust(spread(tmp, y, z),na.rm=TRUE)
